import Flutter
import UIKit

// SceneDelegate is kept empty intentionally.
// Push notification channel is managed by AppDelegate directly
// (window-based setup without scene lifecycle).
class SceneDelegate: FlutterSceneDelegate {
}
